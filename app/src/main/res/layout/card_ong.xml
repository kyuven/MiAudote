<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="180dp"
    android:layout_height="250dp"
    android:layout_marginStart="16dp"
    android:layout_marginTop="12dp"
    android:layout_marginBottom="18dp"

    app:cardElevation="7dp"
    app:cardCornerRadius="8dp"

    android:id="@+id/cardView_ong">

    <!-- MUDAR PRA CONSTRAINT -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Imagem da ONG / Símbolo deles -->
        <ImageView
            android:id="@+id/imgOngCard_logo"
            android:layout_width="140dp"
            android:layout_height="100dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="14dp"
            android:contentDescription="@string/ong_logo_img"

            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Linear Layout da Cidade + UF -->
        <LinearLayout
            android:id="@+id/linearLayout"
            android:layout_width="140dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:orientation="horizontal"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/imgOngCard_logo">

            <!-- Cidade -->
            <TextView
                android:id="@+id/txtOngCard_CddOng"
                android:layout_width="70dp"
                android:layout_height="12dp"
                android:layout_marginTop="2dp"


                android:fontFamily="@font/arimo_variable_font_wght"
                android:text="@string/cidade"
                android:textAllCaps="true"
                android:textColor="@color/laranja01"
                android:textSize="10sp" />

            <!-- UF -->
            <TextView
                android:id="@+id/txtOngCard_UfOng"
                android:layout_width="20dp"
                android:layout_height="12dp"
                android:layout_marginTop="2dp"

                android:fontFamily="@font/arimo_variable_font_wght"
                android:text="@string/uf"
                android:textAllCaps="true"
                android:textColor="@color/laranja01"
                android:textSize="10sp"
                android:textStyle="italic" />

        </LinearLayout>

        <TextView
            android:id="@+id/txtOngCard_nome"
            android:layout_width="140dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="12dp"

            android:fontFamily="@font/arimo_variable_font_wght"
            android:gravity="center"
            android:text="@string/nome_ong"
            android:textAllCaps="true"
            android:textColor="@color/laranja07"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxHeight="50dp"

            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/linearLayout" />

        <!-- Categoria da ONG -->

        <!-- Botão para mais informações da ONG -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnOngCard_info"
            android:layout_width="148dp"
            android:layout_height="36dp"
            android:layout_marginBottom="14dp"
            android:layout_gravity="center_horizontal"

            android:background="@drawable/btn_design"

            android:fontFamily="@font/arimo_variable_font_wght"
            android:text="@string/saiba_mais"
            android:textColor="@color/white"
            android:textSize="16sp"

            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>